<?php
if (isset($_POST['total'])) {
    $total = $_POST['total'];
    global $total;
    // Lưu giá trị total vào một biến hoặc session trong tệp PHP này
    // Ví dụ: $total_value = $_POST['total'];

    // Sau đó, bạn có thể sử dụng $total_value trong tệp PHP này hoặc trong các tệp khác
}
?>
